// Code generated by goa v3.5.4, DO NOT EDIT.
//
// helloserver HTTP client CLI support package
//
// Command:
// $ goa gen github.com/pvr1/goatest/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	hellosvcc "github.com/pvr1/goatest/gen/http/hellosvc/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `hellosvc greet
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` hellosvc greet --name "Mollitia sint omnis modi consequatur harum."` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		hellosvcFlags = flag.NewFlagSet("hellosvc", flag.ContinueOnError)

		hellosvcGreetFlags    = flag.NewFlagSet("greet", flag.ExitOnError)
		hellosvcGreetNameFlag = hellosvcGreetFlags.String("name", "REQUIRED", "Name")
	)
	hellosvcFlags.Usage = hellosvcUsage
	hellosvcGreetFlags.Usage = hellosvcGreetUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "hellosvc":
			svcf = hellosvcFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "hellosvc":
			switch epn {
			case "greet":
				epf = hellosvcGreetFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "hellosvc":
			c := hellosvcc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "greet":
				endpoint = c.Greet()
				data, err = hellosvcc.BuildGreetPayload(*hellosvcGreetNameFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// hellosvcUsage displays the usage of the hellosvc command and its subcommands.
func hellosvcUsage() {
	fmt.Fprintf(os.Stderr, `The hello service
Usage:
    %[1]s [globalflags] hellosvc COMMAND [flags]

COMMAND:
    greet: Greet implements greet.

Additional help:
    %[1]s hellosvc COMMAND --help
`, os.Args[0])
}
func hellosvcGreetUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] hellosvc greet -name STRING

Greet implements greet.
    -name STRING: Name

Example:
    %[1]s hellosvc greet --name "Mollitia sint omnis modi consequatur harum."
`, os.Args[0])
}
